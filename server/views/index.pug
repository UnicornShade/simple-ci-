<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title Document
  body
    div
    div
      h3 Builds
      table: tbody
        each build in builds
          tr
            td: a(href=`/build/${build.id}`)= build.id
            td= build.status

